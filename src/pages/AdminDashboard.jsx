import React from 'react';

    function AdminDashboard() {
      return (
        <div>
          <h1>Admin Dashboard</h1>
          {/* Add components for content management, user management, etc. */}
        </div>
      );
    }

    export default AdminDashboard;
